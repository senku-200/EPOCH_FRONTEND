import MainScheduleComponent from "@/Components/ScheduleComponents/MainScheduleComponent";
import React from "react";

const page = () => {
  return (
    <section className="h-auto md:min-h-screen mb-10 bg-black">
      <MainScheduleComponent />
    </section>
  );
};

export default page;
